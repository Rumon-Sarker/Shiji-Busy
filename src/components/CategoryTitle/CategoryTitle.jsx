const CategoryTitle = ({ title }) => {
    return (
        <div className="text-center text-3xl font-semibold uppercase text-blue-500 my-6">
            <h1>{title}</h1>
        </div>
    );
};

export default CategoryTitle;